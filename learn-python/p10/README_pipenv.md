# pip

[venv](./README.md) | pipenv

## 安装pipenv

```bash
python -m pip install pipenv
```

## `pipenv`安装依赖

```bash
python -m pipenv install requests
```

## 查看虚拟环境的路径

```bash
python -m pipenv --venv
```

## 激活虚拟环境

```bash
python -m pipenv shell
```

## 去激活虚拟环境

```bash
exit
```